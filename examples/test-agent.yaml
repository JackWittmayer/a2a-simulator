name: test-agent

model:
  name: test

systemPrompt: Test agent

skills: []

filesystem:
  rootDir: /workspace
  tree:
    - name: hello.txt
      content: Hello from the test agent!

container:
  baseImage: node:22-slim
  entrypoint: ["cat", "/workspace/hello.txt"]
